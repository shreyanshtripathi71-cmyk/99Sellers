import AdminSettings from "@/components/admin/AdminSettings";

export const metadata = {
  title: "Settings - Admin | 99Sellers",
};

const SettingsPage = () => {
  return <AdminSettings />;
};

export default SettingsPage;
